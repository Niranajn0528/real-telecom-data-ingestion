# Project Overview

This project automates the ingestion of telecom operational events.

Instead of manually collecting logs from multiple systems, the workflow validates and standardizes incoming events before storing them for AI processing.

The repository demonstrates enterprise-grade data engineering concepts using low-code automation.